<%
entries = []
blog.articles.each do |article|
  entry = {
    title: article.title,
    url: article.url,
    content: article.body,
    type: 'article'
  }
  entries << entry
end
%><%=entries.to_json %>
